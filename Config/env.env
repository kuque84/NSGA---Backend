// Exportamos un objeto con la configuración del entorno
module.exports = {
    // El puerto en el que se ejecutará el servidor
    PORT: 3000,

    // La llave secreta que se utilizará para la encriptación
    llave: '@Eregis84',
};